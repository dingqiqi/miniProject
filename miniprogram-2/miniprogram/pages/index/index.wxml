<!--index.wxml-->
<view class="container">

	<view class = "book-container" wx:for="{{books}}" wx:key="key">
		<image class="book_avatar" src="{{item.image}}"></image>
		<view><text>{{item.title}}</text></view>
	</view>

</view>